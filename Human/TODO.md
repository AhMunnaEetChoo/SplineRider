# TODO

- editor improvement: the camera position for edit mode should be cached and returned to after exiting play / testing mode. When we first load a level we find a decent camera position that tries to capture both start and end.

- A new Inbuilt_levels folder that I can use to store levels created in the editor and exported. This is the built in list and replaces the hardcoded levels in levels.js